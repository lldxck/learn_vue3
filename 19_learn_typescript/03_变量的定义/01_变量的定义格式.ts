var name: string = "jack";
let age: number = 18;
const height = 1.88;

// string: typescript中定义变量的字符串类型
// String: 是ECMAScript中定义的一个类


// 默认情况下进行赋值时，会将赋值的值类型，作为该变量的类型，这个过程叫做类型推导（推断）
let message = "hello";
// message是string类型，赋值number类型会报错
// message=123

export {};
